``` bash
for a in $(seq 1 1 9)
do 
    echo "dn: cn=VPN,ou=group,dc=ldap,dc=chummy,dc=finalexam,dc=ncku
    changetype: modifly
    delete: memberUid
    memberUid: VPN0$a" |
    ldapadd -x -D "cn=admin,dc=ldap,dc=chummy,dc=finalexam,dc=ncku" -w finalexam
    ldapdelete -x -D "cn=admin,dc=ldap,dc=chummy,dc=finalexam,dc=ncku" -w finalexam "cn=VPN0$a,ou=group,dc=ldap,dc=chummy,dc=finalexam,dc=ncku"
done
```
