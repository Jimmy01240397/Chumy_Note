``` bash
for a in $(seq 1 1 9)
do
    echo "dn: cn=VPN0$a,ou=people,dc=ldap,dc=chummy,dc=finalexam,dc=ncku
objectClass: top
objectClass: account
objectClass: posixAccount
objectClass: shadowAccount
cn: VPN0$a
uid VPN0$a
userPassword: $(slappasswd -s finalexam)
loginShell: /bin/bash
uidNumber: 200$a
gidNumber: 2000
homeDirectory: /home/VPN0$a
" |
    ldapadd -x -D "cn=admin,dc=ldap,dc=chummy,dc=finalexam,dc=ncku" -w finalexam
    echo "dn: cn=VPN,ou=group,dc=ldap,dc=chummy,dc=finalexam,dc=ncku
changetype: modify
add: memberUid
memberUid: VPN0$a" | 
    ldapmodify -x -D "cn=admin,dc=ldap,dc=chummy,dc=finalexam,dc=ncku" -w finalexam
done
```
